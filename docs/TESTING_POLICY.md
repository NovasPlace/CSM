# Testing Policy

- `npm test` is the production verification gate for supported repository behavior.
- `npm run test:wip` runs experimental and in-development capabilities.
- WIP failures do not block production verification unless the work is promoted into the supported test suite.
